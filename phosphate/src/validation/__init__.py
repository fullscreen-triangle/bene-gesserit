# phosphate/src/validation — Python validation suite for the BPS membrane-computing series.
# Each sub-module validates the quantitative claims of one companion paper.
# Run `python -m validation.run_all` from phosphate/src to generate results JSON.
